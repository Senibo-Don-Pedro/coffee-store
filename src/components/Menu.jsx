import React from "react"
import MenuCard from "../layouts/MenuCard"

const Menu = () => {
  return (
    <section>
      <h1>Our Menu</h1>

      <div>
        <MenuCard />
      </div>
    </section>
  )
}

export default Menu
